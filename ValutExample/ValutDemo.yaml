---
- name: Ansible Vault Test Playbook
  hosts: localhost
  vars_files:
    - secrets.yaml
  tasks:
    - name: Display the secret password
      ansible.builtin.debug:
        msg: "User is {{ ansible_user }} and password is {{ ansible_password }}"
