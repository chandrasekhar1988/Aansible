---
- name: Ansible Vault Test Playbook
  hosts: web
  vars_files:
    - secrets.yaml
  tasks:
    - name: Task 1 - Display User
      ansible.builtin.debug:
        msg: "The database user is {{ myuser }}"
